{
  "log_dir": "./logs",
  "filename_pattern": "sensors_%Y%m%d.csv",
  "buffer_size": 1000,
  "rotate_every_hours": 24,
  "max_size_mb": 20,
  "rotate_after_lines": 1000,
  "retention_days": 30,
  "network": {
    "host": "localhost",
    "port": 6000,
    "timeout": 1.0,
    "retries": 5
  }
}